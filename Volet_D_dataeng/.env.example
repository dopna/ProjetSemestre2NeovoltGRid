# Production (docker-compose) : mot de passe de la base
POSTGRES_PASSWORD=change_me

# --- Execution locale SANS docker (test rapide sur SQLite) ---
# DATABASE_URL=sqlite:///./neovolt.db
# NEOVOLT_DATA_DIR=./donnees
